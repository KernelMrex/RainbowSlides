import React from "react";

export function bindKeys(deleteObject: () => void, copyElement: (event?: React.MouseEvent<HTMLElement>) => void, pasteElement: (event?: React.MouseEvent<HTMLElement>) => void)
{
}